# Windows版 Folimeld 負荷テスト

## 実施状況

2026-09-08（日本時間）。事前確認のみ実施。指定された入力PDFが存在しないため、負荷テストは未実施。以下は性能測定結果ではなく、入力確認の記録である。

## 対象

- 実行ファイル: `C:\Users\takum\Downloads\source\Folimeld\dist\Folimeld.exe`
- バージョン: WindowsのFileVersion情報で `0.3.2` を確認。アプリ画面での照合は未実施。
- 実行ファイルサイズ: 74,065,754 bytes
- 実行ファイルSHA-256: `E3690616CCE522A457C93F5C15D2002B8354A79C84A299BF50597A25B58D8880`
- 指定入力: `C:\Users\takum\Downloads\source\Folimeld\testdata\load\_test\_1100\_pages.pdf`
- 出力先: [build/load-test/](../build/load-test/)（作成済み。保存PDFは未生成）

## 事前確認の方法と結果

PowerShellの `Get-Item` で実行ファイルのサイズ・FileVersionと指定入力の存在を確認し、`Get-FileHash`（SHA-256）で実行ファイルを識別した。指定入力への `Get-Item` は「パスが存在しない」エラーとなった。これはテスト環境の入力パスの問題であり、Folimeldの不具合としては扱わない。

`testdata` 配下のファイル一覧から、代替候補として次のPDFを確認した。指定ファイルとの同一性は確認できていないため、まだテストには使用していない。

- パス: `C:\Users\takum\Downloads\source\Folimeld\testdata\load_test_1100_pages.pdf`
- サイズ: 153,434,540 bytes
- SHA-256: `84B5B3A15BD86BAE94D8CBA671B0D2CD6DC99535CFC9D44CF4767A1ACA119E38`
- 実際のページ数・内容: 未確認（ファイル名から確定しない）

入力PDFの書き換え、アプリの起動・編集・保存は行っていない。

## 確認項目

| 項目 | 結果 |
| --- | --- |
| PDFを開いて表示が完了するまでの時間 | 未計測 |
| 読み込み中と表示後のメモリ使用量 | 未計測 |
| 先頭・中間・末尾へのスクロール応答 | 未確認 |
| 複数ページの選択・回転・移動の所要時間 | 未計測 |
| サムネイルサイズ変更時の応答 | 未確認 |
| 別名保存の時間・再読込後の内容 | 未確認 |
| フリーズ・クラッシュ・アプリのエラー | 未確認。未実施のため「なし」と判定できない |

## 再開時の計測方法（予定・未実施）

入力PDF確定後、元ファイルのハッシュを保存し、Windows実行ファイルをGUIで操作する。操作送信から完了状態の確認までの経過時間を測定し、画面確認の間隔を含む観測値であることを明記する。読み込み中・表示後の対象プロセスのWorking SetとPrivate Bytesを時刻付きで記録し、サンプリング間隔と対象PIDを残す。

先頭・中間・末尾への移動、複数ページの選択・回転・移動、サムネイルサイズ変更を行い、対象ページと操作手順を記録する。別名保存先は `build/load-test/` とし、再読込でページ数・ページ順・回転を照合する。最後に元PDFのハッシュ不変を確認する。

不具合が見つかった場合は、対象ファイル、操作順、期待結果、実際の結果を再現手順として先に報告する。
